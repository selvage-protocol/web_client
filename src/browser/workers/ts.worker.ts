/**
 * The TypeScript/JavaScript language worker: what makes those modes
 * understanding rather than colouring. The module sets its own message handler
 * on import; this entry only names it for the bundler.
 */
import 'monaco-editor/esm/vs/language/typescript/ts.worker.js';
