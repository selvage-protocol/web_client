/**
 * The editor's fallback worker (diff computation and the like). The module sets
 * its own message handler on import; this entry only names it for the bundler.
 */
import 'monaco-editor/esm/vs/editor/editor.worker.js';
